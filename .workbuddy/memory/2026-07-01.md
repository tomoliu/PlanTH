# 2026-07-01 今日摘要

- 角色表新增 初始体力/初始金钱 列（灵梦6/40，魔理沙5/60）；界面动态读取显示
- 敌人属性表/配置表建立；`tools/` → `xls2json/`；JSON 输出到 `data/json/`
- 敌人 AI（八向靠近+射击 2s 间隔），删除四角 ⚡ 发射器；敌人移速 240
- SaveManager 存档系统（3槽，`%Documents%/PlanTH/Saves/`）
- 难度选择重构：居中箭头 ◀▶，0-10 切换，上限受存档等级约束，说明文本来自配置表
- GM 全局 `\` 键 + 难度操纵按钮 + Toast 堆叠反馈（所有场景）
- 主菜单存档切换确认对话框 + 继续游戏（ESC 暂停流转，last_scene_path）
- 战斗 ESC 暂停菜单（返回游戏/返回主菜单）
- 箭头边界透明隐藏（modulate.a=0）+ disabled；GM 按钮 GridContainer 横向 5 列
- 上下文精简：MEMORY.md 浓缩项目状态，日日志仅保留摘要

## 2026-07-02

### 战斗伤害系统 + GameOver UI
- 子弹/敌人碰触改为扣 1 HP（不再直接死亡），HP=0 才触发死亡
- 击中玩家的子弹直接移除（避免同颗子弹连续扣血）
- 死亡时 `last_scene_path = ""`，主菜单“继续游戏”按钮自动隐藏
- GameOver 双阶段面板：
  1. “是否重新开始本关？” → 否/是
  2. 选“否”后显示得分/HP/难度 + 3 空白槽 + 重新开始/新游戏/返回主菜单
- “是”或“重新开始”→ `reload_current_scene`；“新游戏”→ 角色选择；“返回主菜单”→ 主菜单

### GameOver 细节优化
- 确认弹窗按钮顺序改为【是】【否】（是在左）
- 敌人碰触伤害加入 1s CD（`_enemy_contact_cooldowns` 数组），与射击 CD 独立；CD 期间碰触仅弹开不扣血
- 结算窗口不显示 HP 数据（`result_hp_label.visible = false`）
- 确认弹窗 10s 倒计时，超时自动选“否”进入结算。显示橙色 `"Xs 后自动结算..."` 提示

### ESC 暂停菜单：放弃按钮 + 键盘导航
- “返回游戏”和“返回主菜单”中间增加“放弃”按钮
- 放弃 → 立即触发 `_on_player_death()`，进入游戏结束流程
- 暂停时支持 ↑/↓ 键在 3 个按钮间循环切换
- 被选中的按钮高亮（绿色 `Color(0.4, 1.0, 0.4)`），未选中白色
- Enter (`ui_accept`) 触发当前高亮按钮的 pressed
